{% extends 'base.html.twig' %}

{% block title %}Actionnaire{% endblock %}

{% block body %}
<div class="text-center">
    <h1>Actionnaire</h1>
</div>
    <table class="table table-responsive actionnaire">
        <tbody>
            <tr >
                <th>Id</th>
                <td>{{ actionnaire.id }}</td>
            </tr>
            <tr>
                <th>NomStructure</th>
                <td>{{ actionnaire.nomStructure }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ actionnaire.logo }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ actionnaire.dateCreation ? actionnaire.dateCreation|date('m-d-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateModification</th>
                <td>{{ actionnaire.dateModification ? actionnaire.dateModification|date('m-d-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
<div class="my-5">
    <a  class="btn btn-back" href="{{ path('app_actionnaire_index') }}">revenir à la liste</a>

    <a class="btn btn-update " href="{{ path('app_actionnaire_edit', {'id': actionnaire.id}) }}">Modifier</a>
</div>
    {{ include('actionnaire/_delete_form.html.twig') }}
{% endblock %}
